class City:

    def __init__(self, name, lat, long):
        self.name = name
        self.lat = lat     # latitude  (corresponds to y-axis)
        self.long = long   # longitude (corresponds to x-axis)
